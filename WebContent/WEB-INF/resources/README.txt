## Notes
* This is the folder where the application configuration files are located.
* Application uses java servlet 3+ library where web.xml file is no longer necessary. Settings are configured inside the Java classes.
